import React from "react";
import {AppBar, Box} from "@mui/material";

const Navbar = () => {
    const sections = ['Home', 'About me', 'Contact me'];

    return (
        <Box>
            <AppBar>

            </AppBar>
        </Box>
    );
}

export default Navbar;